## 数据集准备与预处理流程

### 1. 重命名数据集文件夹
将 `Q4-Dataset` 目录下的 `训练集` 文件夹重命名为 `train_set`。

### 2. 执行数据格式转换
运行以下脚本将数据集转换为 YOLO 格式：
```bash
python convert_to_yolo.py
3. 执行分层数据集划分
运行以下脚本进行分层采样划分训练集和验证集：

bash
python stratified_split.py
目录结构说明
项目目录结构如下图所示：
https://image.png

text