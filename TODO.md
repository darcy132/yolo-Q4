现在已经添加了数据增强脚本，可以先增强部分数据，再训练模型观察效果。  
目前的 baseline 是 YOLOv26l 模型，但由于模型规模较大，打算先用 n 模型训练看看是否有提升。  

接下来将使用 YOLOv26n 模型在 dataset 上进行训练，参数设置为 imgsz 640，训练 150 轮，先查看结果。  
之后再用增强后的数据以相同参数训练一次，对比是否有效果提升。如果增强有效，再将其应用于 YOLOv6l 模型进行训练。